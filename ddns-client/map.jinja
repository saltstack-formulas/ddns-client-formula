{% set map = salt['grains.filter_by']({
    'Debian': {
        'pkgs': ['dnsutils', 'wget'],
        'script_dir': '/usr/local/bin',
        'user': 'nobody',
        'group': 'nogroup',
        'mode': '700',
        'cronjob_dir': '/etc/cron.d',
    },
}, grain='os', merge=salt['pillar.get']('ddns_client:lookup')) %}
